import React from 'react'

const SecClass = (props) => {
return(
    <div>
 <h3>After 1 month govt takes {props.class} and result will annowce on {props.time}</h3>
</div>
)
}
export default SecClass; 